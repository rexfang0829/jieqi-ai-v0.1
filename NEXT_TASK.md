# Next Task

## 下一輪目標

建議下一輪仍維持 Phase 1 / Phase 1.5 小範圍工作，優先做「實戰輸入 UI 穩定度檢查」。

## 為什麼要做

目前已經有棋盤交叉點、合法步提示、局面編輯、同步上一手、翻子快捷鍵、長按修正、被吃子角落顯示。下一步可以先確認這些實戰輸入流程在桌機與手機都不互相干擾。

## 具體可做功能

- 檢查被吃子 overlay 在手機版是否太靠近可點擊棋子。
- 檢查長按修正選單、同步上一手、一般走棋是否有互相誤觸。
- 整理剩餘亂碼 UI 文案，但不改規則與 AI。

## 不要做哪些超出範圍的事

- 不做首頁。
- 不做棋譜回放。
- 不做 Threat Map。
- 不做 AI VS AI。
- 不做 AI 重寫。
- 不做後端。
- 不做資料庫。
- 不做 Belief State。
- 不做 Monte Carlo。
- 不做 OCR。
- 不做 Ponder。
- 不做自動截圖辨識。
- 不做大型重構。

## 完成標準

- `npm test` 通過。
- `npm run build` 通過。
- 更新 `CODEX_STATUS.md` 與 `NEXT_TASK.md`。
- 測試與 build 通過後 commit 並 push。
